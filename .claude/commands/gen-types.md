# Regenerate Supabase TypeScript types

Run: `npx supabase gen types typescript --project-id $SUPABASE_PROJECT_ID > src/lib/types/database.ts`

Replace $SUPABASE_PROJECT_ID with the project ref from your Supabase dashboard URL.

This must be run after every migration change.
